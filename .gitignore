# Dossiers externes (hors git)
# Les données et vidéos sont stockées dans le dossier parent
# pour éviter les conflits lors des mises à jour git
# ../tv_data/      (progression, alarmes)
# ../movies/       (fichiers vidéo)

# Dépendances
node_modules/
__pycache__/
*.pyc

# Fichiers temporaires
*.tmp
.DS_Store